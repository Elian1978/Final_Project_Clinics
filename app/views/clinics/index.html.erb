<p id="notice"><%= notice %></p>

<button type="button" class="btn btn-lg btn-danger" disabled>Clinicas Disponibles</button>

<!-- 
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Email</th>
      <th>Adress</th>
      <th>Information</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clinics.each do |clinic| %>
      <tr>
        <td><%= clinic.name %></td>
        <td><%= clinic.phone %></td>
        <td><%= clinic.email %></td>
        <td><%= clinic.adress %></td>
        <td><%= clinic.information %></td>
        <td><%= link_to 'Show', clinic %></td>
        <td><%= link_to 'Edit', edit_clinic_path(clinic) %></td>
        <td><%= link_to 'Destroy', clinic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->




<!-- INICIO CARDS -->
<div class="card-deck">
<div class="row row-cols- row-cols-md-3">

<% @clinics.each do |clinic| %>

      <div class="col-md-6">
        <div class="card h-100">
          <div class="card-body">
            <%= image_tag clinic.logo if clinic.logo.attached? %>
            <h4 class="card-text">Clínica: <%= clinic.name %></h4> 
            <h5 class="card-text">Teléfono: <%= clinic.phone  %></h5>
            <h5 class="card-text">Correo: <%= clinic.email %></h5>
            <h5 class="card-text">Dirección: <%= clinic.adress %><br>Servicios: <%= clinic.information %></h5>
             <%= link_to 'Obtener Cita', new_patient_session_path %>
          </div>
        </div>
      </div>
  <% end %>
</div>