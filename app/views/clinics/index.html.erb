<p id="notice"><%= notice %></p>

<h2>Clinics</h2>
<!-- 
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Email</th>
      <th>Adress</th>
      <th>Information</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clinics.each do |clinic| %>
      <tr>
        <td><%= clinic.name %></td>
        <td><%= clinic.phone %></td>
        <td><%= clinic.email %></td>
        <td><%= clinic.adress %></td>
        <td><%= clinic.information %></td>
        <td><%= link_to 'Show', clinic %></td>
        <td><%= link_to 'Edit', edit_clinic_path(clinic) %></td>
        <td><%= link_to 'Destroy', clinic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->


<!-- INICIO CARDS -->
<div class="card-deck">
<div class="row row-cols-1 row-cols-md-3">
  <% @clinics.each do |clinic| %>
      <div class="col-md-4">
        <div class="card h-200">
          <div class="card-body">
            <h4 class="card-text">Nombre: <%= clinic.name %></h4> <br>
            <h5 class="card-text">Teléfono: <%= clinic.phone %>Correo: <%= clinic.email %></h5>
            <h5 class="card-text">Dirección: <%= clinic.adress %><br>Servicios: <%= clinic.information %></h5>
          </div>
        </div>
      </div>
  <% end %>
</div>