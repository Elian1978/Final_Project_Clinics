<p id="notice"><%= notice %></p>

<h2>Notas Importantes</h2>

<table>
  <thead>
    <tr>
      <th> <input class="controls" placeholder="Mensaje"></th>
      <th> <input class="controls" placeholder="Razón de Consulta"></th>
      <th> <input class="controls" placeholder="Paciente"></th>
      <th> <input class="controls" placeholder="Doctor"></th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @notes.each do |note| %>
      <tr>  
        <td><%= note.message %></td>
        <td><%= note.consult_reason %></td>
        <td><%= note.patient.first_name %></td>
        <td><%= note.doctor.name%></td>
        <td><%= link_to 'Editar', edit_note_path(note), class:"text-white"  %></td>
        <td ><%= link_to 'Mostrar', note, class:"text-white"  %></td>
        <td><%= link_to 'Eliminar', note, method: :delete, data: { confirm: 'Are you sure?' }, class:"text-white" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>


<%= link_to 'CREAR NUEVA NOTA', new_note_path, class:"btn btn-primary" %>

  
